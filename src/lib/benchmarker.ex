defmodule Benchmarker do
  @moduledoc """
  Benchmarker keeps the contexts that define your domain and business logic.

  Domain modules live under `Benchmarker.*` (e.g. `Benchmarker.Benchmarks`).
  The web layer is under `BenchmarkerWeb.*`.
  """
end

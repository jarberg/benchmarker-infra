# NOTE: Replace these values with real secrets via your secret manager (e.g.
# External Secrets Operator pulling from AWS Secrets Manager / Vault). This
# file is committed only as a schema reference — do NOT put real secrets here.
apiVersion: v1
kind: Secret
metadata:
  name: benchmarker-secrets
  namespace: benchmarker
type: Opaque
stringData:
  # echo -n "ecto://user:pass@db.example.com:5432/benchmarker" | base64
  DATABASE_URL: "ecto://postgres:postgres@postgres.benchmarker.svc.cluster.local:5432/benchmarker"
  # mix phx.gen.secret
  SECRET_KEY_BASE: "REPLACE_ME_AT_LEAST_64_BYTES_LONG_AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA"
